name: Weekly Rebuild

on:
  schedule:
    # Every Sunday at 1:00 AM UTC
    - cron: '0 1 * * 0'
  workflow_dispatch:  # Allow manual trigger

jobs:
  build:
    uses: ./.github/workflows/build.yml
    secrets: inherit
